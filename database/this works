#include <iostream>
#include <set>
#include <vector>
#include <string>

using namespace std;

int main() {
	set< vector<string> > a;
	vector<string> b = {"asdf", "foo", "one"};
	vector<string> c = {"jkl", "bar", "two"};
	a.insert(b);
	a.insert(c);

	for (set<vector<string>>::iterator it = a.begin(); it != a.end(); ++it) {
		cout << it->at(0) << " " << it->at(1) << " " << it->at(2) << endl;
	}
	return 0;
}
